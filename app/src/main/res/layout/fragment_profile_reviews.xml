<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorBackground"
    android:padding="@dimen/padding_standard">

    <!-- Shown only to eligible customers (role=customer + confirmed booking) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnLeaveReview"
        style="@style/Widget.HandyProject.Button.Primary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/padding_standard"
        android:text="Leave a Review"
        android:visibility="gone" />

    <!-- Average rating summary card -->
    <com.google.android.material.card.MaterialCardView
        style="@style/Widget.HandyProject.Card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/padding_standard">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/padding_standard"
            android:gravity="center">

            <TextView
                android:id="@+id/tvAverageRating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="–"
                android:textSize="@dimen/text_size_display"
                android:textStyle="bold"
                android:textColor="@color/colorPrimary"
                android:fontFamily="sans-serif" />

            <RatingBar
                android:id="@+id/ratingBarSummary"
                style="@style/Widget.AppCompat.RatingBar.Small"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:isIndicator="true"
                android:numStars="5"
                android:stepSize="0.1"
                android:rating="0"
                android:progressTint="@color/colorGold"
                android:layout_marginTop="@dimen/margin_xsmall" />

            <TextView
                android:id="@+id/tvReviewCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/margin_xsmall"
                android:text="No reviews yet"
                android:textSize="@dimen/text_size_caption"
                android:textColor="@color/colorTextSecondary"
                android:fontFamily="sans-serif" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- Real reviews list (fills remaining space) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvReviews"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:visibility="gone" />

    <!-- Empty state (same weight slot — only one is visible at a time) -->
    <TextView
        android:id="@+id/tvEmptyReviews"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="No reviews yet"
        android:textSize="@dimen/text_size_body"
        android:textColor="@color/colorTextSecondary"
        android:fontFamily="sans-serif"
        android:visibility="visible" />

</LinearLayout>
